# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp

# Logs / scratch
*.log
.claude/

# Next.js / Node
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.env*.local
# Backups written by `npm run otp:setup` — these hold real secrets, and the
# rule above does not match them because the filename no longer ends in .local.
.env*.local.bak-*
